export interface PhotoItem {
    photoId: string
    userId: string
    description: string
    createdAt: string
    updatedAt: string
    photoUrl?: string
    likes?: number
    views?: number
  }
  